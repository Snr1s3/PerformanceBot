name: Notify Me

on:
  workflow_run:
    workflows: ["Test and Build", "Deploy mdBook site to Pages"]
    types:
      - completed
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram notification
        run: |
          TITLE="🔔 ${{ github.repository }} - Commit: ${{ github.event.workflow_run.head_commit.message }}"
          if [ "${{ github.event.workflow_run.conclusion }}" = "success" ]; then
            STATUS="✅ Workflow '${{ github.event.workflow_run.name }}' succeeded."
          else
            STATUS="❎ Workflow '${{ github.event.workflow_run.name }}' failed."
          fi
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="${TITLE}   ${STATUS}"
